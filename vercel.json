{
  "buildCommand": "cd apps/self-hosted/web && npm install && npm run build",
  "outputDirectory": "apps/self-hosted/web/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}